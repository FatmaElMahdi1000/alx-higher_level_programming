#!/usr/bin/python3
def multiple_returns(sentence):
    lenght = len(sentence)
    if sentence == "":
        None
        else:
            tuple = (str(lenght) + sentence[0])
            return tuple
